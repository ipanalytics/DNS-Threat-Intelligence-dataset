# Output Schemas

Primary outputs are plain text lists, CSV, JSONL, Parquet-compatible records,
Markdown reports, and dashboard JSON. Pydantic models in `dnsintel.models`
define the canonical schema for evidence and indicators.

